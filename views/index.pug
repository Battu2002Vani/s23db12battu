extends layout

block content
  h1= title
  p Welcome to #{title} I am Vani Battu
  button(class ="btn btn-success" onclick="goFaster()") Click me 
  br 
  br
  div(id="faster") Increase the speed
  a(href="/book") show book page
   script.
      var mph = 20;
      var change = 1;

      function goFaster() {
        mph += change;
        change += 1;
        document.getElementById("faster").innerHTML = `MPH ${mph} with change ${change}`;
      }

